"use strict";
// coding challenges 1
/*
type Props = {
  weight: number;
  height: number;
};

const Mark: Props = {
  weight: 78,
  height: 1.69,
};

const John: Props = {
  weight: 92,
  height: 1.95,
};

const CalculateBMI = (weight: number, height: number): number => {
  return weight / height ** 2;
};

const MarkBMI = CalculateBMI(Mark.weight, Mark.height);
const JohnBMI = CalculateBMI(John.weight, John.height);
 */
//coding challenges 2
/*
const MarkHigherJohn = MarkBMI > JohnBMI;

if (MarkHigherJohn) {
  console.log(`MarkBMI is ${MarkBMI} higher than JohnBMI`);
} else {
  console.log(`JohnBMI is ${JohnBMI} higher than MarkBMI`);
}
*/
// coding challenges 3
// const DolphinsData = [96, 108, 89];
// const KoalasData = [96, 108, 89];
// const CalculateAverage = (Data: number[]): number => {
//   const Average = Data.reduce((a, b) => a + b);
//   return Average / Data.length + 1;
// };
// const DolphinsAverage = CalculateAverage(DolphinsData);
// const KoalasAverage = CalculateAverage(KoalasData);
// if (DolphinsAverage > KoalasAverage) {
//   console.log("Dolphins wins");
// } else if (KoalasAverage > DolphinsAverage) {
//   console.log("Koalas wins");
// } else {
//   console.log("draw");
// }
// coding challenges 4
// const CalculateTips = (value) => {
//     return value > 50 && value < 300
//         ? `The bill was ${value}, the tip was ${value * (15 / 100)}, and the total value ${value + (value * 15) / 100}”`
//         : `The bill was ${value}, the tip was ${value * (20 / 100)}, and the total value ${value + (value * 20) / 100}”`;
// };
// console.log(CalculateTips(400));
//# sourceMappingURL=index.js.map
